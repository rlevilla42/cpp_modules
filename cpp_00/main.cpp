#include "PhoneBook.class.hpp"
#include "Contact.class.hpp"

int main(void)
{
    std::string input;
    PhoneBook   book;
    std::cout << "Write 'ADD' to add a contact to the book contact"<< endl << "Write 'SEARCH' to display a contact information"<< endl << " and write 'EXIT' if you want to exit the program (it will delete all the data)" << endl << "Now write bro: ";
    std::cin >> input
}